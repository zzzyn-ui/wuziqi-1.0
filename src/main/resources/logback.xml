<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>./logs/gobang-server.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>./logs/gobang-server.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>100MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>10GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>

    <!-- Netty日志级别 -->
    <logger name="io.netty" level="INFO" />
    <logger name="io.netty.handler.codec.http.websocketx" level="DEBUG" />

    <!-- MyBatis日志级别 -->
    <logger name="org.apache.ibatis" level="INFO" />

    <!-- Redis日志级别 -->
    <logger name="redis.clients" level="INFO" />

    <!-- GameService日志级别 - 临时设为INFO用于诊断匹配问题 -->
    <logger name="com.gobang.service.GameService" level="INFO" />
</configuration>
